<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <title></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">

  </head>
  <body>

    <div class="container my-5">
      <form method="post" action="/signup" enctype="multipart/form-data">

        {% if errors.global%}
          <div class="alert alert-danger" role="alert">
            t('{{errors.global.msg}}')
          </div>
        {% endif %}

        <div class="form-group">
          <label for="cv">CV</label>
          <input class="form-control {% if errors.filename%}is-invalid{% endif %}" id="cv" name="cv" type="file">
          <div class="invalid-feedback">
            t('{{ errors.filename.msg }}')
          </div>
        </div>

        <div class="form-group">
          <label for="emailAddress">Email address</label>
          <input value="{{ values.emailAddress | default('ee') }}" class="form-control {% if errors.emailAddress%}is-invalid{% endif %}" id="emailAddress" name="emailAddress" aria-describedby="emailHelp" placeholder="Enter email">
          <div class="invalid-feedback">
            t('{{ errors.emailAddress.msg }}')
          </div>
          <small id="emailHelp" class="form-text text-muted">We'll never share your email with anyone else.</small>
        </div>

        <div class="form-group">
          <label for="password">Password</label>
          <input value="{{ values.password | default('pp')  }}" type="password" class="form-control {% if errors.password%}is-invalid{% endif %}" id="password" name="password" placeholder="Password">
          <div class="invalid-feedback">
            t('{{ errors.password.msg }}')
          </div>
        </div>

        <button type="submit" class="btn btn-primary">Submit</button>
      </form>

      <p class="mt-5 mb-0">
        <code>errors</code>
        object:</p>
      <pre><code>
{{ errors | dump(2) }}
      </code></pre>

      <p class="mt-5 mb-0">
        <code>values</code>
        object:</p>
      <pre><code>
{{ values | dump(2) }}
      </code></pre>

      <p class="mt-5 mb-0">
        <code>headers</code>
        object:</p>
      <pre><code>
{{ headers | dump(2) }}
      </code></pre>

    </div>

  </body>
</html>